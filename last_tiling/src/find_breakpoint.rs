use last_tiling::Contigs;
use last_tiling::EncodedRead;

#[derive(Debug)]
struct CriticalRegion {}

fn critical_regions(reads: &[EncodedRead], contigs: &Contigs) -> Vec<CriticalRegion> {
    vec![]
}

